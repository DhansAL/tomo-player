export const Header = ()=>{
    return (
        //TODO: make an env file and put production|dev thingy and make a config folder in backend for that
        <div>-----------------------------------</div>
    )
}